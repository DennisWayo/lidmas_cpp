# Shor [[9,1,3]] CSS Code (Example)

Files in this folder:

- `hx.txt` and `hz.txt`: parity-check matrices (dense 0/1 format)
- `lx.txt` and `lz.txt`: logical operator supports
- `spec.yaml` and `spec.json`: spec files referencing the matrices

Format rules:

- Use only 0/1 entries
- Separate entries with spaces or commas
- One row per line
- Lines starting with `#` or `//` are ignored
